[{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main()\r\n{\r\n    char str[100];\r\n    fgets(str, sizeof(str), stdin);\r\n\r\n    int n = strlen(str);\r\n    int counted[n]; \r\n    for (int i = 0; i < n; i++)\r\n        counted[i] = 0;\r\n\r\n    int maxFreq = 0, maxcharacter = str[0];\r\n    for(int i = 0; str[i] != '\\0'; i++)\r\n    {\r\n        if (counted[i]) \r\n            continue;\r\n\r\n        int count = 0;\r\n        for(int j = 0; str[j] != '\\0'; j++)\r\n        {\r\n            if(str[i] == str[j])\r\n            {\r\n                count++;\r\n                counted[j] = 1;\r\n            }\r\n        }\r\n        if (count > maxFreq) \r\n        {\r\n            maxFreq = count;\r\n            maxcharacter = str[i];\r\n        }\r\n        \r\n    }\r\n    \r\n    if(maxFreq != 0)\r\n        printf(\"%c\",maxcharacter);\r\n    else \r\n        printf(\"%c\",str[n-1]);\r\n\r\n}\r\n","language":"c","time":"2025-04-16T12:03:30.451Z","isCorrect":false},{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main()\r\n{\r\n    char str[100];\r\n    fgets(str, sizeof(str), stdin);\r\n\r\n    int n = strlen(str);\r\n    if (str[n - 1] == '\\n') \r\n    {\r\n        str[n - 1] = '\\0';\r\n        n--;\r\n    }\r\n\r\n    int counted[n]; \r\n    for (int i = 0; i < n; i++)\r\n        counted[i] = 0;\r\n\r\n    int maxFreq = 0;\r\n    char maxChar = '\\0';\r\n\r\n    for (int i = 0; i < n; i++)\r\n    {\r\n        if (counted[i] || str[i] == ' ') \r\n            continue;\r\n\r\n        int count = 1;\r\n        for (int j = i + 1; j < n; j++)\r\n        {\r\n            if (str[i] == str[j])\r\n            {\r\n                count++;\r\n                counted[j] = 1;\r\n            }\r\n        }\r\n\r\n        if (count > maxFreq)\r\n        {\r\n            maxFreq = count;\r\n            maxChar = str[i];\r\n        }\r\n    }\r\n\r\n    if (maxFreq > 0)\r\n        printf(\"%c\", maxChar);\r\n    else\r\n        printf(\"%c\",str[n - 1]);\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-04-16T12:06:05.689Z","isCorrect":false},{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main()\r\n{\r\n    char str[100];\r\n    fgets(str, sizeof(str), stdin);\r\n\r\n    int n = strlen(str);\r\n    int counted[n]; \r\n    for (int i = 0; i < n; i++)\r\n        counted[i] = 0;\r\n\r\n    int maxFreq = 0, maxcharacter = str[0];\r\n    for(int i = 0; str[i] != '\\0'; i++)\r\n    {\r\n        if (counted[i]) \r\n            continue;\r\n\r\n        int count = 0;\r\n        for(int j = 0; str[j] != '\\0'; j++)\r\n        {\r\n            if(str[i] == str[j])\r\n            {\r\n                count++;\r\n                counted[j] = 1;\r\n            }\r\n        }\r\n        if (count > maxFreq) \r\n        {\r\n            maxFreq = count;\r\n            maxcharacter = str[i];\r\n        }\r\n        \r\n    }\r\n    if(maxFreq != 0)\r\n        printf(\"%c\",maxcharacter);\r\n    else \r\n        printf(\"%c\",str[n-1]);\r\n\r\n}\r\n","language":"c","time":"2025-04-16T12:52:59.337Z","isCorrect":false},{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main() {\r\n    char str[100];\r\n    int freq[256] = {0};\r\n\r\n    fgets(str, sizeof(str), stdin);\r\n\r\n    for (int i = 0; str[i] != '\\0'; i++) \r\n        freq[(int)str[i]]++;\r\n\r\n    int max = 0;\r\n    char mostFreq;\r\n    for (int i = 0; i < 256; i++) \r\n    {\r\n        if (freq[i] > max) \r\n        {\r\n            max = freq[i];\r\n            mostFreq = i;\r\n        }\r\n    }\r\n    printf(\"%c\", mostFreq);\r\n}\r\n","language":"c","time":"2025-04-25T08:24:57.644Z","isCorrect":false},{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main() {\r\n    char str[100];\r\n    int freq[256] = {0};\r\n\r\n    fgets(str, sizeof(str), stdin);\r\n\r\n    for (int i = 0; str[i] != '\\0'; i++) \r\n        freq[(int)str[i]]++;\r\n\r\n    int max = 0;\r\n    char mostFreq;\r\n    for (int i = 0; i < 256; i++) \r\n    {\r\n        if (freq[i] > max) \r\n        {\r\n            max = freq[i];\r\n            mostFreq = i;\r\n        }\r\n    }\r\n    if(mostFreq != '\\0')\r\n        printf(\"%c\", mostFreq);\r\n    else\r\n        printf(\"%c\",str[99]);\r\n}\r\n","language":"c","time":"2025-04-25T08:28:25.106Z","isCorrect":false},{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main() {\r\n    char str[100];\r\n    int freq[256] = {0};\r\n\r\n    fgets(str, sizeof(str), stdin);\r\n    str[strcspn(str, \"\\n\")] = '\\0';\r\n\r\n    for (int i = 0; str[i] != '\\0'; i++) \r\n        freq[(int)str[i]]++;\r\n\r\n    int max = 0;\r\n    char mostFreq = '\\0';\r\n\r\n    for (int i = strlen(str) - 1; i >= 0; i--) \r\n    {\r\n        if (freq[(int)str[i]] >= max) \r\n        {\r\n            max = freq[(int)str[i]];\r\n            mostFreq = str[i];\r\n        }\r\n    }\r\n\r\n    if (mostFreq != '\\0')\r\n        printf(\"%c\\n\", mostFreq);\r\n\r\n}\r\n","language":"c","time":"2025-04-25T08:29:47.869Z","isCorrect":false},{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main() {\r\n    char str[100];\r\n    int freq[256] = {0};\r\n    char mostFreq = '\\0';\r\n    int max = 0;\r\n\r\n    fgets(str, sizeof(str), stdin);\r\n\r\n    int len = strlen(str);\r\n    if (len > 0 && str[len - 1] == ' ')\r\n        str[len - 1] = '\\0';\r\n\r\n    for (int i = 0; str[i] != '\\0'; i++)\r\n        freq[(int)str[i]]++;\r\n\r\n    for (int i = 0; str[i] != '\\0'; i++) \r\n    {\r\n        if (freq[(int)str[i]] > max) \r\n        {\r\n            max = freq[(int)str[i]];\r\n            mostFreq = str[i];\r\n        } \r\n        else if (freq[(int)str[i]] == max && str[i] < mostFreq)\r\n            mostFreq = str[i];\r\n    }\r\n\r\n    printf(\"%c\", mostFreq);\r\n}","language":"c","time":"2025-04-25T08:31:17.653Z","isCorrect":false},{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main() {\r\n    char str[1000];\r\n    fgets(str, sizeof(str), stdin);\r\n\r\n    int freq[256] = {0}; \r\n    int len = strlen(str);\r\n    for (int i = 0; i < len; i++) \r\n    {\r\n        if (str[i] != '\\0' && str[i] != ' ' && str[i] != ' ')\r\n            freq[(int)str[i]]++;\r\n    }\r\n\r\n    char mostFrequentChar = '\\0';\r\n    int maxFrequency = 0;\r\n\r\n    for (int i = 0; i < 256; i++) \r\n    {\r\n        if (freq[i] > maxFrequency) \r\n        {\r\n            maxFrequency = freq[i];\r\n            mostFrequentChar = (char)i;\r\n        } \r\n        else if (freq[i] == maxFrequency && freq[i] > 0 && (char)i < mostFrequentChar) \r\n                mostFrequentChar = (char)i;\r\n    }\r\n\r\n    printf(\"%c\", mostFrequentChar);\r\n}\r\n","language":"c","time":"2025-04-25T08:32:46.670Z","isCorrect":false},{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main() {\r\n    char str[100];\r\n    fgets(str, sizeof(str), stdin);\r\n\r\n    int freq[256] = {0}; \r\n    int len = strlen(str);\r\n    for (int i = 0; i < len; i++) \r\n    {\r\n        if (str[i] != '\\0' && str[i] != ' ' && str[i] != ' ')\r\n            freq[(int)str[i]]++;\r\n    }\r\n\r\n    char mostFrequentChar = '\\0';\r\n    int maxFrequency = 0;\r\n\r\n    for (int i = 0; i < 256; i++) \r\n    {\r\n        if (freq[i] > maxFrequency) \r\n        {\r\n            maxFrequency = freq[i];\r\n            mostFrequentChar = (char)i;\r\n        } \r\n        else if (freq[i] == maxFrequency && freq[i] > 0 && (char)i < mostFrequentChar) \r\n                mostFrequentChar = (char)i;\r\n    }\r\n    if(mostFrequentChar != '\\0')\r\n        printf(\"%c\", mostFrequentChar);\r\n    else \r\n        printf(\"%c\",str[99]);\r\n}\r\n","language":"c","time":"2025-04-25T08:33:49.793Z","isCorrect":false},{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main() {\r\n    char str[100];\r\n    fgets(str, sizeof(str), stdin);\r\n\r\n    int freq[256] = {0};\r\n    int len = strlen(str);\r\n\r\n    for (int i = 0; i < len; i++) \r\n        freq[(int)str[i]]++;\r\n\r\n    char mostFrequentChar = '\\0';\r\n    int maxFrequency = 0;\r\n\r\n    for (int i = 0; i < 256; i++) \r\n    {\r\n        if (freq[i] > maxFrequency && (char)i != ' ') \r\n        {\r\n            maxFrequency = freq[i];\r\n            mostFrequentChar = (char)i;\r\n        } \r\n        else if (freq[i] == maxFrequency && maxFrequency > 0 && (char)i < mostFrequentChar && (char)i != ' ') \r\n            mostFrequentChar = (char)i;\r\n    }\r\n\r\n    printf(\"%c\", mostFrequentChar);\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-04-29T12:54:40.071Z","isCorrect":false},{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main() {\r\n    char str[100];\r\n    fgets(str, sizeof(str), stdin);\r\n\r\n    int freq[256] = {0};\r\n    int len = strlen(str);\r\n\r\n    if (len > 0 && str[len - 1] == ' ') \r\n    {\r\n        str[len - 1] = '\\0';\r\n        len--;\r\n    }\r\n\r\n    for (int i = 0; i < len; i++) \r\n        freq[(int)str[i]]++;\r\n\r\n    char mostFrequentChar = '\\0';\r\n    int maxFrequency = 0;\r\n\r\n    for (int i = 0; i < 256; i++) \r\n    {\r\n        if (freq[i] > 0) \r\n        {\r\n            if (freq[i] > maxFrequency) \r\n            {\r\n                maxFrequency = freq[i];\r\n                mostFrequentChar = (char)i;\r\n            } \r\n            else if (freq[i] == maxFrequency && (char)i < mostFrequentChar) \r\n                mostFrequentChar = (char)i;\r\n        }\r\n    }\r\n\r\n    printf(\"%c\", mostFrequentChar);\r\n\r\n    return 0;\r\n}","language":"c","time":"2025-04-29T12:56:43.333Z","isCorrect":false}]